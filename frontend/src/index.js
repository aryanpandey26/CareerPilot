import React from "react";
import ReactDOM from "react-dom/client";
import AutoRedirect from "./AutoDirect";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <AutoRedirect />
  </React.StrictMode>,
);
